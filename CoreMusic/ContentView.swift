import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Воспоминания")
                .font(.cmScreenTitle)
                .padding(.horizontal, Spacing.lg)
                
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
